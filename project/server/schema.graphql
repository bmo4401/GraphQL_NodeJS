### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Folder {
  id: String!
  name: String!
  notes: [Note!]!
}

type Mutation {
  CreateNote(content: String!, folderId: ID!): Note!
}

type Note {
  _id: ID!
  content: String!
  folderId: ID!
}

type Query {
  folder: [Folder!]!
  folderId(folderId: String!): Folder!
  noteId(noteId: String!): Note!
  notes(folderId: String!): [Note]!
}